# Served-tab fixtures

Seventeen frozen runs — eleven agentfootprint 9.94.0 charts, one agentfootprint
9.101.1 chart (`findings-ledger.json`, 0.61.0), one agentfootprint 9.106.0
chart (`ontology.json`, 0.64.0), one agentfootprint 9.109.1 chart
(`coverage.json`, 0.65.0), one agentfootprint 9.110.0 chart
(`proof-map.json`, 0.66.0), one agentfootprint 9.111.0 chart
(`story-marks.json`, 0.67.0 — the one file with a FOURTH key, `trace`, the
AgentThinkingUI trace `agentThinkingTrace()` built beside the recording) and
one plain footprintjs 9.23.1
chart — `{ snapshot, events, structure }` as `recordRun`
froze them (the chart's `events` is `[]`), generated by `generate.ts`, never
hand-authored. Since 9.90.0 the agent charts DECLARE their milestones as tags
(`CommitBundle.tags` / `structure.tags`), which is what the tag legend and the
picker read. Regenerating on 9.94.0 (0.52.0) added exactly one key path to
every agent fixture — `receipt.cache.strategy` (`'*'`) — beside runIds, hashes
and timestamps; `llmcall.json` gained a whole receipt (agentfootprint 9.91.0
made `LLMCall` mint), so the receipt-less arm moved to `no-receipt.json`.
Regenerate all with `npx tsx test/served/fixtures/generate.ts`; regenerate one
with `npx tsx test/served/fixtures/generate.ts <name>` (the other files are not
touched — every run mints a fresh runId, so a regenerated fixture never has the
bytes it had).

| file | shape | what it exercises on the tab |
|---|---|---|
| `flat-dynamic-tools.json` | Agent · `reactMode: 'dynamic'` · two tools · call → done | two epochs; every hashed row **Verified** — system, pieces, messages and both tool schemas; `temperature: 0.25` on the receipt |
| `dynamic-grouped.json` | the same turn · `'dynamic-grouped'` | the epoch's pieces live in the turn's inner log; the mount stop IS the epoch |
| `llmcall.json` | `LLMCall` chart | mints a receipt (9.91.0) with `cache.strategy: null` (9.93.0) → rows **Verified**, the strategy line reads *no cache strategy*, `provider-defaults` is the only gap — the `cache-transform` card is NOT drawn; `omittedForAttention` absent on a 9.93+ receipt → count 0 |
| `no-receipt.json` | the same chart, `recordReceipt: false` | mints none → `no-receipt-on-chart` with `cause: 'no-receipt-committed'`; `cache-transform` still raised (no receipt can say); every row **Reconstructed**; **Not on record** for receipt-only fields and for the attention drops |
| `paused-resumed-no-base.json` | pause + resume, then `initialState` DELETED | `no-fold-base`; the resumed leg is epoch 2 with no epoch 1 in this recording; the system row disagrees and stays **Reconstructed** (the gap excuses a short aggregate), the one rebuilt message pairs with its receipt row by key and is **Verified**; receipt-only counts |
| `tool-forced.json` | `outputSchema(…, { strategy: 'tool-forced' })` | the forced tool named and marked; its schema row absent; `forced-tool-schema` gap |
| `tool-set-changes.json` | one stepped skill: `lookup` → `charge` | SINCE PREVIOUS names `charge` added and `lookup` removed at epoch 2; `currentSkillId` and `stepPointer` on the FOLD. The system text is the SAME at both epochs (the step note leads the tool's description, not the prompt) — SINCE PREVIOUS reads **unchanged** for it |
| `hidden-skills.json` | `PermissionPolicy` role `support` may not see `payroll` | `hiddenSkillIds: ['payroll']` on the FOLD at every stop from the first call; nowhere on any receipt |
| `instructions-move.json` | one tool, `lookup`, and two `defineInstruction`s: `before-lookup` (active until a tool has run) and `after-lookup` (on `lookup`'s return) | the SYSTEM TEXT differs between epochs — epoch 1 is `support bot` + `Look the order up before you answer.`, epoch 2 is `support bot` + `The order is on record. Answer from it.`; SINCE PREVIOUS reads **changed** · pieces entered 1 · left 1 · tools added 0 · removed 0; the word diff (removed `Look … before you answer.`, added `… is on record. Answer from it.`) and the **Show diff** / **Hide diff** toggle; every piece **Verified** at both epochs. The "diff not computed" label is pinned on this fixture with an in-test edit (both instruction texts replaced by 3 000 words differing throughout, past `DIFF_CELL_CAP`) |

| `tagged-chart.json` | a plain footprintjs chart: `seed → call-llm[milestone:llm-turn] → normalise → route[milestone:decision, audit] → finish[audit]`, no agent events | the TAG LEGEND on a name the domain does not classify: `audit` prints its raw name beside the milestone labels, declared in the structure and hit twice; `tagStops(['audit'])` keeps `route` and `finish` and folds `normalise` into the turn before it |
| `window-evicts.json` | `.window(slidingWindow({ keepRecentTurns: 1 }))`, one tool, three calls | epoch 3's receipt carries `omittedForAttention: { count: 2, hashes }` — the assistant tool call and its result that left at iteration 3's head — and both hashes are epoch 2's `messages.entries[].hash` (epoch 1 served only the user turn): two withheld message edges, *last served on epoch 2*. Epochs 1 and 2: `none-on-receipt` → count 0, nothing withheld. The generator checks the pairing on the real run |
| `wrap-up.json` | `maxIterations: 2`, a model that always calls a tool | epoch 3 is the wrap-up call: `tools.withheld: 'wrap-up'` on the receipt and the view, rendered verbatim; no tool row and no count of the tab's own |
| `findings-ledger.json` | an ARMED agent (`.findings()`, agentfootprint 9.101.0) · one tool, `lookup` · batch 1 = four calls with a basis each; batch 2 = two calls, the first declaring the four results (`c1` **fact** · `c2` **noise** · `c3` **ruled-out** with its `line` · `c4` **open** with `settles`); a JSON answer under `.outputSchema()` standing on `c5` as a second **fact** on `c1`'s subject and predicate with a different value | the `findingsLedger` key ENTERS at the first tool-calls stop (basis rows) and CHANGES at the second (standing rows); the answer's peel writes ONE `conflict` row with two witnesses (`c1`, `c5`); `c6` has no standing row — UNDECLARED, in `toolResults` — never `open`; epoch 3's wire carries the `source: 'findings'` system piece and two collapsed tickets (`c2`, `c3`); epochs 1 and 2 carry neither. Generated ALONE on 9.101.1 (0.61.0); the other twelve `cmp`-equal after |
| `ontology.json` | an agent that DECLARED a map (`.ontology(defineOntology(...))`, agentfootprint 9.106.0) · one tool, `lookup` · two calls then an answer · the map `fleet` v`1`: sources `inventory` (`configured: true`, a coverage sentence) and `telemetry` (nothing said about configuration); terms `port` (held by `inventory` **via `lookup`** — the registered tool — with its own coverage sentence), `port_error_rate` (unit `errors/min`, held by `telemetry`), `optic` (aliases, held by BOTH sources), `maintenance_window` (held by NO source); relations `port_error_rate measured-on port` and `optic seated-in port` (with a meaning) | the run constant `ontology` (`{ id, version, hash, spec }`) is written ONCE by seed and is the same at every stop after; the `source: 'ontology'` system piece is on the served view at all three epochs; the Ontology view draws two sources, three held terms, one unheld term under `known, not held here`, four holdings (one labelled `lookup`), two relations. Generated ALONE on 9.106.0 (0.64.0); the other thirteen `cmp`-equal after |
| `coverage.json` | an agent whose tools DECLARE their coverage (agentfootprint 9.109.1) · three tools, three calls in three iterations, then an answer · `zone_membership` returns `coverage(verdict, {...})` — `checked` `shq-fab-a: the live fcns database` and `window: the last 24h` (`why` the active zoneset is read live), `notChecked` `the archived zoneset history`, `cannotCover` `ports on the peer fabric` and `host-side multipathing` (each with a `why`); `flogi_for_port` returns `absent({ what: 'FLOGI entries on fc1/3', ... })` — `checked` the SAME fcns entry word-for-word and `window: the last 24h` under a DIFFERENT `why` (FLOGI history retention), `cannotCover` the SAME peer-fabric entry with the same `why`, a `tryInstead`; `port_state` returns a bare string · NO `.findings()` · `.limitsTravelWithTheAnswer()` on | the tracked key `coverageDeclared` ENTERS at the first tool-calls stop (one `ledger` row, `set`) and CHANGES at the second (one `absence` row with `lookedFor`, `append`); the third call declares nothing; no `findingsLedger` anywhere — the Reasoning lens draws the Coverage band ALONE; the `agentfootprint.agent.turn_end` event's `payload.finalContent` carries the library's own composed block (Checked 3 — the two `window` entries both kept, Not checked 1, Cannot cover 2 — the shared entry once), the oracle the band's boundary is pinned against. Generated ALONE on 9.109.1 (0.65.0); the other fourteen `cmp`-equal after |
| `proof-map.json` | an agent with BOTH doors armed (`.findings()` + `.namesAndNumbersFromEvidence({ posture: 'assist' })`, agentfootprint 9.110.0) and a small `.ontology(...)` (`fabric` v`1`: sources `fcns` and `zoneset`; terms `port` held by `fcns` **via `port_state`**, `zone` held by `zoneset` **via `zone_lookup`** — the agent's two registered tools) · three calls in three iterations, then a JSON answer under `.outputSchema()` · c1 (`port_state`) declared **noise** on c2's `_findings.previous`; c2 (`zone_lookup`) declared **fact** (one assertion) on the answer; c3 (`port_state`) named by nothing — UNDECLARED · the answer quotes `fc1/7` (carried ONLY by c1), `fc2/9` and `Z_ESX07` (carried by the fact c2) and `fc9/9` (carried by nothing) | the ledger ENTERS at the first tool-calls stop (c1's basis) and grows at each: c1's standing lands with c2's basis at the second, c3's basis at the third; at the answer's route stop it gains c2's standing (`declaredOn: 'answer'`) and ONE `contingent` row `{ declaredOn: 'answer', value: 'fc1/7', carriers: [{ c1, noise }] }`, and the state gains `unsupportedValues` (`fc9/9`, posture `assist`, `revised: false`); the run constant `ontology` is seeded once; `toolResults` at the end holds c3 alone, `history` names every result's tool. The Proof map draws the answer standing on c2, the contingent edge answer → c1, c3 dashed, two tools, two sources. Generated ALONE on 9.110.0 (0.66.0); the other fifteen `cmp`-equal after |
| `story-marks.json` | the `proof-map` run without the ontology and with an `agentThinkingTrace()` recorder WATCHING (`.watch(att)`, agentfootprint 9.111.0) · c1 (`port_state`, exploratory, a `proposition` and a `predicts`, no `expect`) declared **noise** on c2's `_findings.previous`; c2 (`zone_lookup`, direct, `expect: 'high'`) declared **fact** + `sought: true` (one assertion) on the JSON answer; c3 (`port_state`, exploratory) named by nothing — UNDECLARED · the answer quotes `fc1/7` (carried ONLY by c1 → one `contingent` row on the answer) and `fc9/9` (carried by nothing → `unsupportedValues`, posture `assist`) · the file's fourth key `trace` is the player's trace: `prompt · ask c1 · return c1 · ask c2 · return c2 · ask c3 · return c3 · answer`, every ask/return beat stamped with its `toolCallId`, the prompt and the answer with none | `storyMarks(trace, record)` at the run's end: c1's ask `[hypothesis] [the proposition clipped to 40] [predicts: …]`, c1's return `[noise]`, c2's ask `[direct] [expect high]`, c2's return `[fact] [sought]`, c3's ask `[hypothesis]`, c3's return `[undeclared]`, the answer `[stood on 1 · noise 1] [contingent 1] [unsupported 1]`; at the last llm-turn stop before the answer c1 reads `noise` already, c2 `undeclared`, the answer beat nothing. Generated ALONE on 9.111.0 (0.67.0); the other sixteen `cmp`-equal after |

The one edit ever made to a recording is the deleted `initialState` in
`paused-resumed-no-base.json`, and the generator says so at the line it does it.
